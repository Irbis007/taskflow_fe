// import { $api } from "@shared/api";

// const getAllChats = () => {
//   const mutation = $api.useMutation("post", "/api/chats");

//   return {
//     ...mutation,
//     mutateAsync: (data) => mutation.mutateAsync({ body: data }),
//   };
// };
